# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
build/
dist/

# ── Virtual environments ──
.venv/
venv/
env/
ENV/

# ── Streamlit ── (commit config.toml, never commit secrets)
.streamlit/secrets.toml

# ── Runtime cache (regenerated on demand; persisted via Render disk in prod) ──
.cache/

# ── OS / editor ──
.DS_Store
.vscode/
.idea/
*.swp
